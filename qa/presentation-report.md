# Проверка презентационной версии — 2026-09-23

## Автоматические проверки

`python -m unittest discover -s tests -q`: 76 тестов, успешно.
Добавлены регрессии: составной запрос цены/доставки; выбор первой карточки при отсутствии автоматического выбора; подтверждение корзины; фото и изменение количества; виртуальная логистика не меняет реальный лимит; вложенные сертификаты; фильтр категории при поиске.

## Реальное API и модель

Прогон с текущими серверными ключами и каталогом EKT:

| Шаг | Результат | Время |
| --- | --- | --- |
| Лампы для кафешки | 4 реальные карточки | 6,06 с |
| Максимально дешевые + быстро в Алматы | 6 карточек, явно обозначенный демосрок | 3,20 с |
| Добавь первую лампу | Только запрос подтверждения | 0,12 с |
| Да, добавь | Одна позиция в локальной корзине, фото из API | 0,14 с |

Это измерения одного прогона, не гарантия задержки внешних сервисов.

## Ручная проверка UI

Категория «Лампы» выделяется отдельно; 261 позиция в категории локального кэша. Карточка показывает отсутствие сертификата и кнопку менеджера. Добавление → подтверждение → корзина с реальной фотографией. Изменение 1 → 3 → подтверждение возвращает в корзину с количеством 3 и пересчитанной суммой 2 397 ₸ (цена 799 ₸ в момент проверки).

## Границы MVP

В выборке 23 детальных ответов EKT (включая 515291, 33723, 33705) ссылки сертификатов не предоставлены. Реальные документы не созданы и не подменены. Тесты подтверждают отображение ссылок при их наличии в API.

Демологистика — отдельные виртуальные сроки/остатки по Алматы, Астане, Шымкенту и Караганде, помеченные DEMO. Они не разрешают покупку сверх реального остатка. Штатная корзина EKT и внутренняя БД не подключены; прототип не резервирует товары и не оформляет заказ. Полный набор требований к продукционной интеграции требует контракта EKT.
